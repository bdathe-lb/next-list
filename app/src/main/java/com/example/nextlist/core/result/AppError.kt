package com.example.nextlist.core.result

enum class AppError {
    UNAUTHENTICATED,
    PERMISSION_DENIED,
    NETWORK_UNAVAILABLE,
    NOT_FOUND,
    ALREADY_EXISTS,
    GROUP_FULL,
    GROUP_DISSOLVED,
    INVITE_INVALID,
    INVITE_EXPIRED,
    EMAIL_NOT_VERIFIED,
    NOT_ADMIN,
    ADMIN_CANNOT_LEAVE,
    TARGET_NOT_MEMBER,
    CONFLICT,
    VALIDATION,
    RATE_LIMITED,
    UNKNOWN,
}
