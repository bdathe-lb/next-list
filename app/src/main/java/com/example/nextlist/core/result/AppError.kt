package com.example.nextlist.core.result

enum class AppError {
    UNAUTHENTICATED,
    PERMISSION_DENIED,
    NETWORK_UNAVAILABLE,
    NOT_FOUND,
    ALREADY_EXISTS,
    GROUP_FULL,
    INVITE_EXPIRED,
    VALIDATION,
    RATE_LIMITED,
    UNKNOWN,
}
