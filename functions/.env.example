# Local Emulator only. Copy to functions/.env.local if you want to override the
# built-in demo secret. Never commit a real production secret.
NEXTLIST_INVITE_SECRET=replace-with-at-least-32-random-characters
NEXTLIST_INVITE_SECRET_VERSION=emulator-v1
